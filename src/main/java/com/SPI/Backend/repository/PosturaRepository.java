package com.SPI.Backend.repository;
import com.SPI.Backend.model.Postura;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PosturaRepository extends JpaRepository<Postura, Long>{
}
